# Practice Problem: Drop missing values
print("🧠 QUESTION: How do you drop rows with missing values in pandas?")
answer = input("Type your answer in code (e.g., df.dropna()): ")

if "dropna" in answer:
    print("✅ Correct! That removes missing rows.")
else:
    print("❌ Hint: Look into the `dropna()` function.")
